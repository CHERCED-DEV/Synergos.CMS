@model Synergos.CMS.Application.Elements.SpacerViewModel
@{
    var cls = string.Join(" ", new[] { Model.BlockClass, Model.DomClass?.ClassList }.Where(s => !string.IsNullOrWhiteSpace(s)));
}
<div class="@cls" data-sg-element="struct-spacer" aria-hidden="true"></div>
