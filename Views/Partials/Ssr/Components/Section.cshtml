@* STUB — not reachable from Block Grid.
   Structural blocks (elementStructSection) are dispatched directly to
   Views/Partials/blockgrid/Components/elementStructSection.cshtml
   via the alias.StartsWith("elementStruct") guard in items.cshtml.
   This partial only exists as a safety fallback for legacy mapper paths. *@
