@*
    Email template — form submission notification.
    Modelo: Synergos.CMS.Web.Services.FormNotificationEmailModel
*@
@model Synergos.CMS.Web.Services.FormNotificationEmailModel
@{
    Layout = "_Layout";
    ViewBag.Title = $"Nueva submission: {Model.FormKey}";
    ViewBag.SiteName = Model.SiteName;
    ViewBag.PreheaderText = $"Recibiste una nueva submission del formulario {Model.FormKey}.";
}
<h2 style="font-size:20px;color:#0f172a;margin:0 0 16px;">
    Nueva submission del formulario <code>@Model.FormKey</code>
</h2>
<table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%" style="background:#f8fafc;border-radius:6px;margin:0 0 24px;">
    <tr>
        <td style="padding:16px;font-size:13px;color:#475569;">
            <div><strong>Recibido:</strong> @Model.ReceivedAtUtc.ToString("yyyy-MM-dd HH:mm:ss") UTC</div>
            <div><strong>Origen:</strong> @Model.Referrer</div>
            <div><strong>IP:</strong> @Model.ClientIp</div>
        </td>
    </tr>
</table>

<table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%" style="border-collapse:collapse;">
    <thead>
        <tr>
            <th colspan="2" style="padding:8px 16px;text-align:left;font-size:12px;font-weight:600;color:#475569;text-transform:uppercase;letter-spacing:0.5px;border-bottom:2px solid #e2e8f0;">
                Campos
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var (key, value) in Model.Fields)
        {
            <tr>
                <th style="padding:8px 16px;text-align:left;font-size:13px;font-weight:600;color:#0f172a;border-bottom:1px solid #e2e8f0;width:30%;vertical-align:top;">
                    @key
                </th>
                <td style="padding:8px 16px;font-size:13px;color:#475569;border-bottom:1px solid #e2e8f0;white-space:pre-wrap;">
                    @value
                </td>
            </tr>
        }
    </tbody>
</table>
