@model Umbraco.Cms.Core.Models.PublishedContent.IPublishedElement
@inject Synergos.CMS.Interfaces.ISynHostEmitter Emitter
@{
    var props = new Dictionary<string, object?>(StringComparer.Ordinal)
    {
        ["stepsJson"] = Model.Value<string>("stepsJson"),
        ["submitEndpoint"] = Model.Value<string>("submitEndpoint"),
        ["allowSkip"] = Model.Value<bool>("allowSkip"),
    };
    var request = new Synergos.CMS.Interfaces.SynHostEmitRequest("form-stepper", props, Model.Value<string>("configOverride"), System.Globalization.CultureInfo.CurrentUICulture);
    var result = await Emitter.EmitAsync(request);
}
@Html.Raw(result.ScriptHtml)
@Html.Raw(result.ElementHtml)
