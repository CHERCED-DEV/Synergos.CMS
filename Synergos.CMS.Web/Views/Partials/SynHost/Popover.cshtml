@model Umbraco.Cms.Core.Models.PublishedContent.IPublishedElement
@inject Synergos.CMS.Interfaces.ISynHostEmitter Emitter
@{
    var props = new Dictionary<string, object?>(StringComparer.Ordinal)
    {
        ["triggerLabel"] = Model.Value<string>("triggerLabel"),
        ["popoverContent"] = Model.Value<string>("popoverContent"),
        ["placement"] = Model.Value<string>("placement"),
    };
    var request = new Synergos.CMS.Interfaces.SynHostEmitRequest("popover", props, Model.Value<string>("configOverride"), System.Globalization.CultureInfo.CurrentUICulture);
    var result = await Emitter.EmitAsync(request);
}
@await Html.PartialAsync("SynHost/_Wrapper", (Model, result.ScriptHtml, result.ElementHtml))