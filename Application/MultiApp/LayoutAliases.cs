namespace Synergos.CMS.Application.MultiApp;

internal static class LayoutAliases
{
    /// <summary>Boolean property on siteRoot that marks the node as the application root.</summary>
    internal const string ApplicationRootFlag = "layoutIsApplicationRoot";

    /// <summary>Culture code property on siteRoot (e.g. "es", "en-US").</summary>
    internal const string DefaultCulture      = "defaultCulture";
}
